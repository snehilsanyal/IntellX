def jira_scraper(**args, **kwargs, verbose = True):
    '''
    Template signature for jira_scraper

    Input:

    Output:
    Arrow format/Parquet/Numpy array dataset (faster load and retrieve)
    
    Mentions dataset size after scraping. 

    Mention usecase here in docs (so that it can be retrieved using help())

    '''
    dataset = ....
    dataset_info = 'Arrow format, {}'.format(dataset.size)


    return dataset, dataset_info
